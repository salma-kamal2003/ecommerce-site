import { BoxShadowHoverDirective } from './box-shadow-hover.directive';

describe('BoxShadowHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxShadowHoverDirective();
    expect(directive).toBeTruthy();
  });
});
